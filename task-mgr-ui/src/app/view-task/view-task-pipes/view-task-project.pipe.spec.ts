import { ViewTaskProjectPipe } from './view-task-project.pipe';

describe('ViewTaskProjectPipe', () => {
  it('create an instance', () => {
    const pipe = new ViewTaskProjectPipe();
    expect(pipe).toBeTruthy();
  });
});
