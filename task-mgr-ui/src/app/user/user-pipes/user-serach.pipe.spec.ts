import { UserSerachPipe } from './user-serach.pipe';

describe('UserSerachPipe', () => {
  it('create an instance', () => {
    const pipe = new UserSerachPipe();
    expect(pipe).toBeTruthy();
  });
});
